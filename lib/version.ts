/**
 * 应用版本号，发版时改这里。
 * 应与 src-tauri/tauri.conf.json 的 version 保持一致。
 */
export const APP_VERSION = "0.1.0";

/**
 * GitHub 仓库路径，格式为 "owner/repo"。
 * 推送到 GitHub 后填入，留空则关闭更新检查。
 *
 * 示例: "your-username/trick-cards"
 */
export const GITHUB_REPO = "";
